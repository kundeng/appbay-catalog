project: default
environment: default
collection: [ai-stack, gpu-apps]
tags:
  tier: compute
  role: llm-inference

upstream:
  source: ./docker-compose.yml
  expose:
    - mistralrs

traits:
  - type: gpu
    variant: nvidia
