project: default
environment: default
collection: [ai-stack]
tags:
  tier: frontend
  role: chat-ui

upstream:
  source: ./docker-compose.yml
  expose:
    - sillytavern

traits:
  - type: ingress
    host: "sillytavern.${{project.DOMAIN}}"
    port: 8000
    service: sillytavern
    exposure: external
