project: default
environment: default
collection:
- automation
tags:
  tier: infra
  role: garbage-collection
upstream:
  source: ./docker-compose.yml
  expose:
  - docker-gc
traits:
- type: backup
  schedule: 0 3 * * *
  retention: 14
  volumes:
  - docker-gc-config
