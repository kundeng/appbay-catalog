project: default
environment: default
collection: [tools]
tags:
  tier: tool

upstream:
  source: ./docker-compose.yml

overlays:
  - when: [ollama]
    services:
      parllama:
        environment:
          - OLLAMA_URL=http://appbay.ollama:11434
