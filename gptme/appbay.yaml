project: default
environment: default
collection: [devtools]
tags:
  tier: tool
  role: cli-tool

upstream:
  source: ./docker-compose.yml

overlays:
  - when: [ollama]
    services:
      gptme:
        environment:
          - OLLAMA_INTERNAL_URL=http://appbay.ollama:11434
