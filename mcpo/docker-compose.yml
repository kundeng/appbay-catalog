services:
  mcpo:
    image: ghcr.io/open-swarm/mcpo:latest
    container_name: mcpo
    restart: unless-stopped
    ports:
      - "${MCPO_PORT:-8000}:8000"
    volumes:
      - mcpo_cache:/app/cache
      - mcpo_data:/app/data

volumes:
  mcpo_cache:
  mcpo_data:
