services:
  mock-openai:
    image: harbor/mock-openai:latest
    container_name: mock-openai
    restart: unless-stopped
    ports:
      - "${MOCK_OPENAI_HOST_PORT:-29350}:8080"
    environment:
      - PORT=8080
