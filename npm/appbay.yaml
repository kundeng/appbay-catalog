project: default
environment: default
collection: [admin, self-hosted]
tags:
  tier: infra
  role: reverse-proxy-manager

upstream:
  source: ./docker-compose.yml
  expose:
    - npm

traits:
  - type: ingress
    host: "npm.${{project.DOMAIN}}"
    port: 81
    service: npm
    exposure: internal
