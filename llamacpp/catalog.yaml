name: llamacpp
description: High-performance LLM inference server using llama.cpp with OpenAI-compatible API
version: latest
category: ai
tags: [inference, llm, gpu]
source:
  type: git
  url: https://github.com/ggerganov/llama.cpp
readiness: raw
required_inputs:
  - name: LLAMACPP_MODEL
    description: Model specifier (e.g. HuggingFace repo or GGUF path)
    type: string
    default: ""
traits_summary:
  - "ingress: external HTTPS"
maintainer: appbay
