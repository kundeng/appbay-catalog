name: opint
description: Open Interpreter CLI agent that executes code locally via OpenAI-compatible API
version: latest
category: ai
tags: [agents, coding, cli]
source:
  type: git
  url: https://github.com/OpenInterpreter/open-interpreter
readiness: raw
required_inputs:
  - name: OPENAI_API_KEY
    description: OpenAI API key or compatible endpoint key
    type: secret
  - name: OPENAI_API_BASE
    description: Base URL for the OpenAI-compatible API
    type: string
traits_summary: []
maintainer: appbay
