name: qbittorrent-vpn
description: Torrent client tunneled through Gluetun VPN container
version: latest
category: media
tags: [torrent, download, vpn, privacy]
source:
  type: local
readiness: augmented
required_inputs:
  - name: DOWNLOADS_DIR
    description: Host path for downloads
    type: path
    default: /data/downloads
traits_summary:
  - "no ingress — uses gluetun network_mode (VPN tunnel)"
  - "backup: daily (config only)"
  - "requires gluetun service running"
  - "no auth trait — no direct web exposure"
maintainer: appbay
