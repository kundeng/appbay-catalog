project: default
environment: default
collection: [self-hosted]
tags:
  tier: app
  role: terminal

upstream:
  source: ./docker-compose.yml
  expose:
    - openterminal

traits:
  - type: ingress
    host: "terminal.${{project.DOMAIN}}"
    port: 8000
    service: openterminal
    exposure: external
