name: openterminal
description: Web-based terminal and notebook environment with configurable package installation
version: latest
category: ai
tags: [tools, terminal, notebook]
source:
  type: git
  url: https://github.com/nicepkg/open-terminal
readiness: raw
required_inputs:
  - name: OPENTERMINAL_API_KEY
    description: API key for Open Terminal access
    type: secret
traits_summary:
  - "ingress: external HTTPS"
maintainer: appbay
