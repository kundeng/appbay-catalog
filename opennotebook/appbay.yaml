project: default
environment: default
collection: [self-hosted]
tags:
  tier: app
  role: notebook

upstream:
  source: ./docker-compose.yml
  expose:
    - opennotebook

traits:
  - type: ingress
    host: "notebook.${{project.DOMAIN}}"
    port: 8502
    service: opennotebook
    exposure: external
