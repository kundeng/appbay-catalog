name: cognee
description: Knowledge graph memory engine for LLMs with RAG, embedding, and graph database support
version: latest
category: ai
tags:
  - rag
  - knowledge-graph
  - memory
  - llm
source:
  type: git
  url: https://github.com/topoteretes/cognee
readiness: augmented
required_inputs:
  - name: OPENAI_API_KEY
    description: OpenAI API key. Issued by OpenAI — appbay cannot generate it.
    type: secret
traits_summary:
  - ingress (API on port 8000)
maintainer: appbay
