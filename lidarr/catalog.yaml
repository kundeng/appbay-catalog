name: lidarr
description: Music management with automatic downloading
version: latest
category: media
tags: [music,pvr,automation]
source:
  type: git
  url: https://github.com/Lidarr/Lidarr
  ref: main
readiness: augmented
required_inputs:
  - name: LIDARR_HOST
    description: Hostname for Lidarr
    type: string
    default: "lidarr.${{project.DOMAIN}}"
traits_summary:
  - "ingress: internal HTTPS"
  - "auth: Authelia forward-auth"
  - "backup: daily (config)"
  - "no secrets — built-in auth"
maintainer: appbay
