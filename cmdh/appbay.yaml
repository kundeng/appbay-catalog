project: default
environment: default
collection: [devtools]
tags:
  tier: tool
  role: cli-tool

upstream:
  source: ./docker-compose.yml

overlays:
  - when: [ollama]
    services:
      cmdh:
        environment:
          - OLLAMA_HOST=http://appbay.ollama:11434
