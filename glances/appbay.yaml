project: default
environment: default
collection: [monitoring]
tags:
  tier: infra
  role: system-monitor

upstream:
  source: ./docker-compose.yml
  expose:
    - glances

traits:
  - type: ingress
    host: "glances.${{project.DOMAIN}}"
    port: 61208
    service: glances
    exposure: internal
