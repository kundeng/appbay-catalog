name: browseruse
description: AI-powered browser automation agent with VNC display for visual debugging
version: latest
category: ai
tags:
  - agents
  - browser
  - automation
source:
  type: git
  url: https://github.com/browser-use/web-ui
readiness: raw
required_inputs:
  - name: OPENAI_API_KEY
    description: OpenAI API key. Issued by OpenAI — appbay cannot generate it.
    type: secret
  - name: ANTHROPIC_API_KEY
    description: Anthropic API key. Issued by Anthropic — appbay cannot generate it.
    type: secret
traits_summary:
  - ingress (web UI on port 7788)
maintainer: appbay
