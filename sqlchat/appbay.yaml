project: default
environment: default
collection: [ai-stack]
tags:
  tier: app
  role: sql-chat

upstream:
  source: ./docker-compose.yml
  expose:
    - sqlchat

traits:
  - type: ingress
    host: "sqlchat.${{project.DOMAIN}}"
    port: 3000
    service: sqlchat
    exposure: external
