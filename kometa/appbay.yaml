project: default
environment: default
collection:
- media
tags:
  tier: app
  role: metadata-manager
upstream:
  source: ./docker-compose.yml
  expose:
  - kometa
traits:
- type: backup
  schedule: 0 3 * * *
  retention: 14
  volumes:
  - kometa-config
