project: default
environment: default
collection: [ai-stack]
tags:
  tier: app
  role: rag-pipeline

upstream:
  source: ./docker-compose.yml
  expose:
    - raglite

traits:
  - type: ingress
    host: "raglite.${{project.DOMAIN}}"
    port: 8000
    service: raglite
    exposure: external
