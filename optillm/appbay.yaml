project: default
environment: default
collection: [tools]
tags:
  tier: tool

upstream:
  source: ./docker-compose.yml

overlays:
  - when: [ollama]
    services:
      optillm:
        environment:
          - OLLAMA_INTERNAL_URL=http://appbay.ollama:11434
  - when: [litellm]
    services:
      optillm:
        environment:
          - OPTILLM_BASE_URL=http://appbay.litellm:4000/v1
