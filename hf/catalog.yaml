name: hf
description: HuggingFace CLI for downloading, managing, and interacting with models and datasets
version: latest
category: ai
tags:
  - cli
  - huggingface
  - models
source:
  type: git
  url: https://github.com/huggingface/huggingface_hub
readiness: raw
required_inputs:
  - name: HF_TOKEN
    description: Hugging Face access token, for gated or private model downloads.
    type: secret
traits_summary: []
maintainer: appbay
