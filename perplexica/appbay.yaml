project: default
environment: default
collection: [ai-stack]
tags:
  tier: app
  role: ai-search

upstream:
  source: ./docker-compose.yml
  expose:
    - perplexica

traits:
  - type: ingress
    host: "perplexica.${{project.DOMAIN}}"
    port: 3000
    service: perplexica
    exposure: external
