project: default
environment: default
collection: [monitoring]
tags:
  tier: infra
  role: image-updates

upstream:
  source: ./docker-compose.yml
  expose:
    - wud

traits:
  - type: ingress
    host: "wud.${{project.DOMAIN}}"
    port: 3000
    service: wud
    exposure: internal
